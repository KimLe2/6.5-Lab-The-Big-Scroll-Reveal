ScrollReveal().reveal('.record', {opacity:0.8, duration:1000, delay:1000});

ScrollReveal().reveal('.house', {distance:'500px', delay:1500});

ScrollReveal().reveal('.swan', {duration:2000, delay:2000});

ScrollReveal().reveal('.canyon', {delay: 3000, easing:'ease-out', duration:3000,});

ScrollReveal().reveal('.park', {scale: 0.85, easing:'ease-in', delay:3500});

/* window.onload = function() {
    ScrollReveal().reveal('site', {delay: 1000}
}

function clickOMatic() {
    alert('Hello!');
  }

  window.onload = function() {

  };

const click = document.getElementById('click');

click.addEventListener("click", (event) => {
  clickOMatic();
});

  let clickOMatic = function() {
    alert('Hello!');
  };

  let clickOMatic = () => alert('Hello!');
  */